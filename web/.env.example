# Copy to .env for local dev or .env.production for prod builds.
# The web app uses this to call the Django backend.

# Local backend
VITE_API_URL=http://127.0.0.1:8000

# Production (Render)
# VITE_API_URL=https://trak-backend-upip.onrender.com

# Optional: allow the dev mock feed when the backend is unreachable.
# VITE_ALLOW_MOCK_FEED=true
