/**
 * Default native-stack options: swipe-from-edge back on iOS/Android, no header bar.
 */
export const defaultStackScreenOptions = {
  headerShown: false,
  gestureEnabled: true,
  fullScreenGestureEnabled: true,
  animation: 'slide_from_right',
};
